{
  "name": "vaultbound-night-shift",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "야간 지령, 보스 프로토콜, 스타일 랭크와 관계 컷신을 갖춘 모바일 우선 픽셀 잠입·금고 복구 인디게임",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.js",
    "simulate": "node scripts/simulate.mjs && node scripts/simulate-advanced.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
